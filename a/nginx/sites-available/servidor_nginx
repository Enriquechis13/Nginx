server {
    listen 80;
    servidor_nginx servidor_nginx.com www.servidor_nginx.com;

    root /var/www/servidor_nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
